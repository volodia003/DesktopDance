# 🤝 Руководство по внесению вклада

Спасибо за интерес к Desktop Dance! Мы приветствуем любой вклад в проект.

## 📋 Как внести вклад

### 🐛 Сообщить о баге

1. Проверьте, не был ли баг уже [зарегистрирован](https://github.com/volodia003/DesktopDance/issues)
2. Создайте новый Issue с меткой `bug`
3. Опишите:
   - Шаги для воспроизведения
   - Ожидаемое поведение
   - Фактическое поведение
   - Скриншоты (если применимо)
   - Версию приложения и ОС

### 💡 Предложить новую функцию

1. Создайте Issue с меткой `enhancement`
2. Опишите:
   - Какую проблему решает эта функция
   - Как вы видите её реализацию
   - Примеры использования

### 🔧 Отправить код

1. **Форкните** репозиторий
2. **Создайте ветку** для вашей функции:
   ```bash
   git checkout -b feature/amazing-feature
   ```
3. **Следуйте стилю кода** проекта
4. **Коммитьте** изменения с понятными сообщениями:
   ```bash
   git commit -m "Add: новая крутая функция"
   ```
5. **Запушьте** в свой форк:
   ```bash
   git push origin feature/amazing-feature
   ```
6. **Откройте Pull Request**

## 📝 Стиль кода

### C# Соглашения

- Используйте **PascalCase** для классов, методов, свойств
- Используйте **camelCase** для локальных переменных
- Используйте **_camelCase** для приватных полей
- Добавляйте комментарии для сложной логики
- Избегайте избыточных комментариев

```csharp
// ✅ Хорошо
private string _characterName;

public void AddCharacter(string name)
{
    _characterName = name;
}

// ❌ Плохо
private string charactername;

public void addcharacter(string name)
{
    charactername = name;
}
```

### Структура файлов

```
Desktop-Dance/
├── Classes/        # Модели и вспомогательные классы
├── Forms/          # UI формы (WinForms)
├── Services/       # Бизнес-логика и сервисы
├── Utility/        # Утилиты и хелперы
└── Resources/      # Ресурсы (изображения, иконки)
```

### Именование

- **Классы**: `CharacterEntity`, `WindowPictureBox`
- **Интерфейсы**: `ICharacterService`
- **Методы**: `AddCharacter`, `SaveSettings`
- **События**: `OnCharacterAdded`, `CharacterRemoved`
- **Константы**: `DEFAULT_SCALE`, `MAX_CHARACTERS`

## 🧪 Тестирование

Перед отправкой PR убедитесь, что:

- [ ] Приложение компилируется без ошибок
- [ ] Нет предупреждений компилятора (или они обоснованы)
- [ ] Приложение запускается и работает корректно
- [ ] Новая функциональность протестирована вручную
- [ ] Не сломана существующая функциональность

### Запуск тестов

```bash
# Сборка
dotnet build

# Запуск (для проверки)
dotnet run
```

## 📦 Коммиты

### Формат сообщений коммитов

```
<тип>: <краткое описание>

<подробное описание (опционально)>

<ссылка на Issue (опционально)>
```

### Типы коммитов

- `Add:` - добавление новой функции
- `Fix:` - исправление бага
- `Update:` - обновление существующей функции
- `Remove:` - удаление кода
- `Refactor:` - рефакторинг без изменения функциональности
- `Docs:` - изменения в документации
- `Style:` - форматирование кода
- `Perf:` - улучшение производительности

### Примеры

```bash
# Хорошо ✅
Add: поддержка видео форматов MP4 и WEBM
Fix: утечка памяти при добавлении персонажей
Update: улучшен UI карточного дизайна
Docs: добавлена инструкция по установке

# Плохо ❌
добавил новое
исправил баг
обновил
```

## 🎯 Приоритеты разработки

### Высокий приоритет
- Исправление критических багов
- Улучшение производительности
- Исправление утечек памяти

### Средний приоритет
- Новые функции из TODO
- Улучшение UI/UX
- Оптимизация кода

### Низкий приоритет
- Рефакторинг работающего кода
- Косметические изменения
- Экспериментальные функции

## 🚫 Что НЕ принимается

- Код, нарушающий стиль проекта
- Изменения без описания/обоснования
- Код с ошибками компиляции
- Изменения, ломающие существующую функциональность
- Коммиты с плохими сообщениями
- Большие PR без предварительного обсуждения

## ❓ Вопросы

Если у вас есть вопросы:

1. Проверьте [README.md](README.md)
2. Проверьте [BUILD.md](BUILD.md)
3. Поищите в [Issues](https://github.com/volodia003/DesktopDance/issues)
4. Создайте новый Issue с меткой `question`

## 📜 Лицензия

Внося вклад в проект, вы соглашаетесь, что ваш код будет лицензирован под той же лицензией, что и проект (см. [LICENSE](LICENSE)).

## 💖 Благодарности

Спасибо всем контрибьюторам проекта! Ваш вклад делает Desktop Dance лучше.

---

**Счастливого кодинга! 🎉**

