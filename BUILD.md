# 🔨 Инструкция по сборке Desktop Dance

## Для разработчиков

### Требования

- Visual Studio 2022 или VS Code
- .NET 7.0 SDK
- Git

### Клонирование и сборка

```bash
# Клонировать репозиторий
git clone https://github.com/ВАШ_ЛОГИН/Desktop-Dance.git
cd Desktop-Dance

# Восстановить зависимости
dotnet restore

# Собрать проект (Debug)
dotnet build

# Запустить проект
dotnet run
```

## Создание релиза

### Вариант 1: Портативная версия (рекомендуется)

Создание самодостаточного исполняемого файла со всеми зависимостями:

```bash
# Сборка для Windows x64
dotnet publish -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:EnableCompressionInSingleFile=true

# Файлы будут в: bin\Release\net7.0-windows\win-x64\publish\
```

**Результат**: Один файл `DesktopDance.exe` размером ~70-80 МБ, не требует установки .NET Runtime.

### Вариант 2: Компактная версия (требует .NET Runtime)

```bash
# Сборка требующая .NET Runtime на целевой машине
dotnet publish -c Release -r win-x64 --self-contained false

# Файлы будут в: bin\Release\net7.0-windows\win-x64\publish\
```

**Результат**: Файлы размером ~500 КБ, но требуется установленный .NET 7.0 Runtime.

### Вариант 3: Установщик (для продвинутых)

Используйте [Inno Setup](https://jrsoftware.org/isinfo.php) или [WiX Toolset](https://wixtoolset.org/) для создания установщика.

## Оптимизация релиза

### Уменьшение размера

```bash
# Включить обрезку неиспользуемого кода (trimming)
dotnet publish -c Release -r win-x64 --self-contained true /p:PublishTrimmed=true /p:PublishSingleFile=true

# Включить ReadyToRun (AOT компиляция)
dotnet publish -c Release -r win-x64 --self-contained true /p:PublishSingleFile=true /p:PublishReadyToRun=true
```

**Внимание**: Тримминг может вызвать проблемы с рефлексией. Тестируйте перед публикацией!

## Подготовка к публикации на GitHub

### 1. Создание архива релиза

```bash
# Windows (PowerShell)
cd bin\Release\net7.0-windows\win-x64\publish
Compress-Archive -Path * -DestinationPath Desktop-Dance-v1.0.0-win-x64.zip
```

### 2. Создание тега и релиза

```bash
# Создать тег
git tag -a v1.0.0 -m "Release v1.0.0"

# Отправить тег на GitHub
git push origin v1.0.0
```

### 3. Публикация релиза на GitHub

1. Перейдите на страницу репозитория на GitHub
2. Нажмите "Releases" → "Create a new release"
3. Выберите созданный тег (v1.0.0)
4. Заполните описание релиза:
   ```markdown
   ## 🎉 Desktop Dance v1.0.0
   
   ### Новые возможности
   - Анимированные персонажи на рабочем столе
   - Поддержка пользовательских GIF
   - Два режима работы (одиночный/множественный)
   - Автозапуск с Windows
   
   ### Установка
   1. Скачайте `Desktop-Dance-v1.0.0-win-x64.zip`
   2. Распакуйте архив
   3. Запустите `DesktopDance.exe`
   
   ### Системные требования
   - Windows 10 или новее
   - .NET 7.0 Runtime (если используете lightweight версию)
   ```
5. Прикрепите ZIP-архив
6. Нажмите "Publish release"

## Структура архива релиза

### Self-contained (портативный)
```
Desktop-Dance-v1.0.0-win-x64.zip
├── DesktopDance.exe        # ~70-80 МБ
└── README.txt              # Краткая инструкция
```

### Runtime-dependent (требует .NET)
```
Desktop-Dance-v1.0.0-win-x64.zip
├── DesktopDance.exe
├── DesktopDance.dll
├── DesktopDance.runtimeconfig.json
├── DesktopDance.deps.json
└── README.txt
```

## Автоматизация сборки

### GitHub Actions (рекомендуется)

Создайте `.github/workflows/release.yml`:

```yaml
name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 7.0.x
    
    - name: Publish
      run: dotnet publish -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true
    
    - name: Create Archive
      run: Compress-Archive -Path bin\Release\net7.0-windows\win-x64\publish\* -DestinationPath Desktop-Dance-${{ github.ref_name }}-win-x64.zip
    
    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: Desktop-Dance-${{ github.ref_name }}-win-x64.zip
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
```

Теперь при создании тега автоматически создастся релиз!

## Тестирование перед релизом

### Чек-лист

- [ ] Приложение запускается без ошибок
- [ ] Все персонажи корректно отображаются
- [ ] Загрузка пользовательских GIF работает
- [ ] Настройки сохраняются и загружаются
- [ ] Автозапуск работает
- [ ] Системный трей функционирует
- [ ] Нет утечек памяти при длительной работе
- [ ] CPU нагрузка в норме (~1-3%)
- [ ] Приложение корректно закрывается

### Тестовые сценарии

1. **Базовый сценарий**:
   - Запустить приложение
   - Добавить персонажа
   - Изменить размер, отзеркалить
   - Перетащить в другое место
   - Перезапустить → проверить что настройки сохранились

2. **Стресс-тест**:
   - Добавить 10+ персонажей
   - Оставить на час работать
   - Проверить потребление памяти

3. **Режимы**:
   - Переключить между одиночным/множественным
   - Проверить что персонажи корректно удаляются/добавляются

## Обновление версии

Не забудьте обновить версию в файле проекта:

```xml
<!-- DesktopDance.csproj -->
<PropertyGroup>
  <Version>1.0.0</Version>
  <AssemblyVersion>1.0.0.0</AssemblyVersion>
  <FileVersion>1.0.0.0</FileVersion>
</PropertyGroup>
```

## Поддержка

При возникновении проблем со сборкой:
1. Проверьте версию .NET SDK: `dotnet --version`
2. Очистите проект: `dotnet clean`
3. Удалите папки `bin` и `obj`
4. Пересоберите: `dotnet build -c Release`

## Полезные ссылки

- [.NET CLI Reference](https://docs.microsoft.com/en-us/dotnet/core/tools/)
- [Publishing Applications](https://docs.microsoft.com/en-us/dotnet/core/deploying/)
- [GitHub Releases](https://docs.github.com/en/repositories/releasing-projects-on-github)

